---
import { alternateLanguagePath, siteConfig } from "../config/site";

type Props = {
  title?: string;
  description?: string;
  image?: string;
  type?: "website" | "article";
  structuredData?: Record<string, unknown>;
  lang?: "de" | "ar";
};

const {
  title = siteConfig.title,
  description = siteConfig.description,
  image = siteConfig.defaultImage,
  type = "website",
  structuredData,
  lang = "de",
} = Astro.props;

const siteUrl = Astro.site;
const pathname = Astro.url.pathname;
const canonical = siteUrl ? new URL(pathname, siteUrl) : undefined;
const absoluteImage = siteUrl ? new URL(image, siteUrl) : undefined;
const pageTitle = title === siteConfig.title ? title : `${title} | ${siteConfig.name}`;
const alternatePath = alternateLanguagePath(pathname, lang);
const locale = lang === "ar" ? "ar_DE" : "de_DE";
---

<title>{pageTitle}</title>
<meta name="description" content={description} />
{canonical && <link rel="canonical" href={canonical.href} />}
{siteUrl && <link rel="alternate" hreflang={lang} href={new URL(pathname, siteUrl).href} />}
{siteUrl && <link rel="alternate" hreflang={lang === "ar" ? "de" : "ar"} href={new URL(alternatePath, siteUrl).href} />}
{siteUrl && <link rel="alternate" hreflang="x-default" href={new URL("/", siteUrl).href} />}
<meta property="og:locale" content={locale} />
<meta property="og:type" content={type} />
<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={description} />
{canonical && <meta property="og:url" content={canonical.href} />}
<meta property="og:site_name" content={siteConfig.name} />
{absoluteImage && <meta property="og:image" content={absoluteImage.href} />}
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta
  property="og:image:alt"
  content={lang === "ar"
    ? "صورة معاينة اجتماعية مُنشأة بالذكاء الاصطناعي لمكتب شاكر للاستشارات"
    : "Mit KI generiertes Social-Preview-Bild von Shaker Consulting"}
/>
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={description} />
{absoluteImage && <meta name="twitter:image" content={absoluteImage.href} />}
<meta
  name="twitter:image:alt"
  content={lang === "ar"
    ? "صورة معاينة اجتماعية مُنشأة بالذكاء الاصطناعي لمكتب شاكر للاستشارات"
    : "Mit KI generiertes Social-Preview-Bild von Shaker Consulting"}
/>
{structuredData && (
  <script is:inline type="application/ld+json" set:html={JSON.stringify(structuredData)} />
)}
